eureka:
  client:
    service-url:
      defaultZone: http://localhost:10000/eureka

spring:
  application:
    name: 'cloud-gateway'
  cloud:
    gateway:
      discovery:
        locator:
          enabled: true
server:
  port: 12000

logging:
  level:
    org.springframework.cloud.gateway: debug
